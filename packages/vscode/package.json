{
  "name": "vscode-yeastbook",
  "displayName": "Yeastbook",
  "description": "A TypeScript notebook powered by Bun. One command, zero config. No Python, no conda, no kernel installs.",
  "version": "0.0.1",
  "publisher": "codepawl",
  "repository": {
    "type": "git",
    "url": "https://github.com/codepawl/yeastbook.git",
    "directory": "packages/vscode"
  },
  "homepage": "https://github.com/codepawl/yeastbook",
  "bugs": {
    "url": "https://github.com/codepawl/yeastbook/issues"
  },
  "license": "MIT",
  "engines": { "vscode": "^1.85.0" },
  "activationEvents": ["onNotebook:yeastbook"],
  "icon": "icon.png",
  "categories": ["Notebooks", "Programming Languages"],
  "keywords": ["typescript", "notebook", "bun", "jupyter", "yeastbook"],
  "main": "./dist/extension.js",
  "contributes": {
    "notebooks": [{
      "type": "yeastbook",
      "displayName": "Yeastbook Notebook",
      "selector": [
        { "filenamePattern": "*.ybk" }
      ]
    }],
    "languages": [{
      "id": "yeastbook",
      "aliases": ["Yeastbook"],
      "extensions": [".ybk"]
    }],
    "commands": [
      { "command": "yeastbook.runAll", "title": "Yeastbook: Run All Cells", "icon": "$(run-all)" },
      { "command": "yeastbook.restartKernel", "title": "Yeastbook: Restart Kernel", "icon": "$(debug-restart)" },
      { "command": "yeastbook.openInBrowser", "title": "Yeastbook: Open in Browser", "icon": "$(globe)" },
      { "command": "yeastbook.exportScript", "title": "Yeastbook: Export as Script" },
      { "command": "yeastbook.stripOutputs", "title": "Yeastbook: Strip Outputs" },
      { "command": "yeastbook.showServerLogs", "title": "Yeastbook: Show Server Logs", "icon": "$(output)" },
      { "command": "yeastbook.selectPythonEnvironment", "title": "Yeastbook: Select Python Environment", "icon": "$(symbol-misc)" }
    ],
    "menus": {
      "notebook/toolbar": [
        { "command": "yeastbook.restartKernel", "group": "navigation/execute@1", "when": "notebookType == yeastbook" },
        { "command": "yeastbook.openInBrowser", "group": "navigation/execute@2", "when": "notebookType == yeastbook" }
      ]
    },
    "configuration": {
      "title": "Yeastbook",
      "properties": {
        "yeastbook.serverPort": {
          "type": "number",
          "default": 0,
          "description": "Fixed port for yeastbook server (0 = auto)"
        },
        "yeastbook.cliPath": {
          "type": "string",
          "default": "",
          "description": "Path to yeastbook CLI (cli.ts). Auto-detected if empty."
        },
        "yeastbook.bunPath": {
          "type": "string",
          "default": "bun",
          "description": "Path to bun executable"
        },
        "yeastbook.autoStartServer": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start server when opening a notebook"
        },
        "yeastbook.kernelStartupTimeout": {
          "type": "number",
          "default": 15000,
          "description": "Timeout in ms for kernel startup (default 15000)"
        }
      }
    }
  },
  "scripts": {
    "build": "bun build src/extension.ts --outdir dist --target node --format cjs --external vscode",
    "package": "rm -rf node_modules && bunx @vscode/vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.0",
    "ws": "^8.18.0"
  }
}
